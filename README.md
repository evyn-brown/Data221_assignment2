Question1: Read from file, find top 10 most frequent words in the file and print the words along with their word count to user.
Question2: Read from a file, find top 10 most frequent bigrams in the file and print the bigram along with their count to user. 
Question3: Read from a file, clean up each line (remove punctuation, remove spaces,make lowercase) and count if there are duplicate lines in the file.
Question4: Read from a file, Make a data frame using columns studytime, internet, absences, of a certain value and read it to csv file, then compute the mean grades of the students in the new data set
Question5: Read from a file, Make a new column, grade_band, based off the students grades. Then uses grade_band to count number of students in each band, their absences and percent with access to internet
Question6: Read from a file, make new column of ViolentCrimes to group the data by, and compute unemployment rate for each category.
Question7: Webscrape to find the main title and first paragraph of at least 50 character, to be printed to the user.
Question8: Webscrape to find all headers within the main content area, then write all headers, which do not contain certain keywords, to a file
Quesiton9: Webscrape to find the first table, fill in missing columns and rows, then print the table to a csv file
Question10:Define a funciton to read a file and keyword from user, then count how many times that keyword is in the file, print out count of keyword and lines on which it appears.
